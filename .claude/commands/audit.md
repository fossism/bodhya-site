Run a site audit on the Bodhya website. Use Playwright to:

1. Navigate to http://localhost:1111 (start `zola serve` first if not running)
2. Check all nav links work
3. Check mobile menu at 375px viewport
4. Check consulting page stat boxes render correctly
5. Check color contrast on hero text
Report findings grouped by severity.